[package]
name = "ldm-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "LDM command-line interface: download, fetch, probe, and manage downloads from the terminal."
authors.workspace = true

[[bin]]
name = "ldm"
path = "src/main.rs"

[dependencies]
ldm-engine = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }
clap = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
ldm-test-server = { path = "../ldm-test-server" }
