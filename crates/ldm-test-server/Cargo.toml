[package]
name = "ldm-test-server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Local HTTP test server for LDM: deterministic files, range support toggles, throttling, failures, redirects, and malicious behaviors."
authors.workspace = true

[dependencies]
tokio = { workspace = true }
axum = "0.8"
http = "1"
bytes = "1"
futures-util = "0.3"
sha2 = "0.10"
hex = "0.4"
rand_chacha = "0.3"
rand_core = "0.6"
serde = { workspace = true }
serde_json = { workspace = true }
base64 = "0.22"
